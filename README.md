# Fingerprint-Detection-System
With startdup En La Parada, we have built a system capable of detecting a person's fingerprint. 

#Credits 
To obtain the fixed-length embeddings of fingerprints, we implemented: https://github.com/tim-rohwedder/fixed-length-fingerprint-extractors. The folder by the name fixed-length-fingerprint-extractors is a clone of the repository we used to be able to install the library in our development environment.
